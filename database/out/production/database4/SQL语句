创建表格：
cj student (id,name,sex,age)
思想：三个主要字段
第一步：解析：cj，看关键字是否匹配。
第二步：检查表名，看数据库中是否存在该表。
第三步：解析后面的字段，用文件操作写入即可


插入数据：
cr student (1,张三,男,18)
cr student (2,李四,女,28)
cr student (3,王五,男,22)
cr student (4,小红,女,20)
cr student (5,小明,男,25)
cr student (6,小张,男,22)
cr student (7,小婷,女,21)
思想：三个主要字段
第一步：解析：cr，看关键字是否匹配。
第二步：检查表名，看数据库中是否存在该表。
第三步：解析后面的字段,首先要把后面的字段解析出来，存放在tableValue中，
从数据中把表中表头字段读出来，存放在tableHead中，先进行list大小对比，如果不一样就提示“字段未匹配！”
大小相等就直接插入。

查询数据：
cx student all 查询所有
cx student id,name,sex,age 查询指定字段
思想：
第一步：判断字段数，三个就是查询所有，四个就是有一个查询条件的，多的就是有多个查询条件。
第二步：查询字段匹配，后面的条件，一个是从表中读取出来的字段，一个是需要查询的字段，扫描看是否存在相等的，
        存在就记录下来字段下标，根据下标来获取数据。大小写严格控制相等
第三步：将表中数据逐一读取出来，根据第二步的下标，将数据存放在另外一个数组中，最后逐个放在list表单中传送出来。

查询指定条件
cx student all age=22
cx student name sex=男
思想：
第一步：四个就是有一个查询条件的
第二步：条件condition为keysql[3]（如age=18），field为key[2]表示要查询的字段名
第三步：先用condition将满足的数据筛选出来，并且将数据存储再map容器中，
之后再利用getConcreteField方法（同上面的第二三步），来获取想要的具体字段。


包含and和or的查询指定条件
cx student all sex=男 and age>=22
cx student all sex=女 or age>19
思想：
第一步：四个以上就是有多个查询条件的
第二步：用selectWhere函数，将表名，两个条件，以及要查询出来的字段和连接符一起放进去。
第三步：and:相同未1，不同为0，逐个遍历，然后放到list中，or:直接put到map列表中，再将map重新存放在list里面


//删除修改：能查到就能删除或者修改，修改是先删除再增加
更改数据：
gx student sex=男 将表格所有数据都修改为性别=男
cx student all
思想：
第一步：先判断有几个关键字，三个关键字的是更新表的所有数据。
超过三个关键字，第四个关键字是“qz”,此后的关键字表示要更新的数据应该满足的条件。
五个关键字的是更新表的满足这个条件的部分数据。七个是包含or或and连接词的更新
第二步：把要修改的数据字段和值存放在map中，其中，map的key是数据字段列下标，value为要修改的新值。
第三步： 将新数据存储在一个新的list中，
存储原则是：列下标相同的则数据更改，其他的依旧是原来的数据值，再将原来表删除，重新创建这个表，将新数据插入。

gx student sex=男 qz id=2
cx student all
思想：
第一步：先判断有几个关键字，五个关键字的是更新表的满足这个条件的部分数据。
第二步：把要修改的数据字段和值存放在map中，其中，map的key是数据字段列下标，value为要修改的新值。
找到要修改的数据，进行修改（同之前步骤），将新的数据也存储在一个新的list集合中。
第三步： 将原来表删除，重新创建这个表，将新数据插入

gx student sex=女,age=20 qz id=4 or id=5
cx student all
思想：和之前一样
只有查询数据的时候调用了不同的方法，所以不再赘述

删除数据：
sc student id=1 删除指定条件的数据
cx student all
思想：
第一步：先判断有几个关键字，三个关键字的是删除表的满足这个条件的部分数据。
第二步：先将整个表格中所有数据存下来放一个list里面，再将满足条件的数据存放在一个map容器中，因为map可以一次存两个值。
第三步： 把要删除的数据和所有的数据进行对比。将不是这个条件数据的保存下来。再删除整个表的数据。重新创建这个表，将数据插入。

sc student id=1 and age>20
cx student all
思想：
第一步：先判断有几个关键字，五个关键字的是删除表的满足这个条件的部分数据。
第二步： 先将整个表格中所有数据存下来放一个list里面，再记录条件字段是哪一列，将这一列数据存储在一个新的list表单里面。
第三步： 把要删除的数据和所有的数据进行对比。将不是这个条件数据的保存下来。再删除整个表的数据。重新创建这个表，将数据插入。

sc student  删除表格所有数据
cx student all
思想：
第一步：两个关键字的是删除这个表的所有数据
第二步： 先将整个表格中表头数据存下来，再删除整个表的数据
第三步： 重新创建这个表，将表头插入。

drop student
思想：
第一步：解析：drop，看关键字是否匹配。
第二步：检查表名，看数据库中是否存在该表。若不存在，直接提示“该表格不存在！”
第三步：直接使用文件流进行删除。


删除表
drop student